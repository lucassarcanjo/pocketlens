import Foundation
import Domain

/// Namespace placeholder for the `LLM` package.
///
/// Real `LLMProvider` protocol + `MockProvider`, `OllamaProvider`, `OpenAIProvider`,
/// and `AnthropicProvider` land here in Phase 5.
/// See `.claude-plans/06-phase-5-llm.md`.
public enum LLM {
    public static let placeholder = "PocketLens.LLM"
}
